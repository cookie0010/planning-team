# PRD 산출물 템플릿

> 이 파일은 `@prd-writer`가 생성하는 PRD의 **표준 구조**입니다.
> 작성 규칙(What/Why only·정량 지표·버전 관리·핸드오프 등)은 `.claude/agents/prd-writer.md`를 따릅니다.
> 아래 구조를 사용자 언어로 그대로 따르세요. 본문 내 주석(`<!-- -->`)은 작성 지침이며, 문서 완성 시 삭제합니다.

---

# PRD: [프로젝트명]

> Project: `[project-slug — Idea Deep-Dive에서 승계]`
> Date: YYYY-MM-DD / Version: v1

<!-- ──────────────────────────────────────────────
이 템플릿의 사용 규칙 (작성 시작 전에 읽고, 문서 완성 시 이 블록은 삭제)

1. 한 문서 = 현재 결정의 스냅샷. 본문에 취소선·"(v2에서 변경)"·구버전 흔적을 남기지 않는다.
   변경 이력은 맨 아래 부록 표 한 줄로만 남기고, 상세는 git/버전 파일로.
2. Why(시장·근거·경쟁·차별화 논증)는 쓰지 않는다. 0절에서 one-pager 링크 한 줄로 대체.
   단, 핵심 가정·성공 지표·기각 기준은 PRD에 남는다 — 전략이 아니라 이 빌드의 인수 조건이므로.
3. PRD ↔ 디자인 경계 테스트: "디자이너가 이걸 다르게 만들면 제품이 틀어지는가?"
   틀어지면 PRD에 쓴다(정보 요구사항·필수 행동·플로우·품질 기준).
   안 틀어지면 쓰지 않는다(레이아웃·색·컴포넌트 선택). 레퍼런스 제품 명시는 OK.
4. [잠정] 태그 규칙: 임의로 채운 값에는 반드시 [잠정]을 붙이고, 그 값을 확정할 OQ 번호에 연결한다.
   [잠정] 없는 문장은 전부 확정 사항으로 읽힌다.
5. OQ 수명 규칙: OQ는 "착수 전(blocking)"과 "설계 단계" 둘로만 구분.
   해소된 OQ는 본문에서 삭제하고 부록 변경 이력에 한 줄 남긴다. 취소선으로 본문에 두지 않는다.
6. 완료 조건은 측정 가능해야 한다. "매끄러운", "직관적인", "빠른" 금지 —
   숫자(N초·N회·N명 중 N명)나 검증 가능한 행동으로 쓴다.
7. 비목표에는 "왜 안 하는지" 한 줄과 "언제 재검토하는지"(v2 후보 / 영구 제외)를 붙인다.
8. 모호한 단위는 본문에서 정의한다 (예: "3액션"이면 액션 1회가 무엇인지).
────────────────────────────────────────────── -->

---

## 0. 요약

[무엇을 만드는지 2~3문장. 이 문단만 읽어도 제품이 그려져야 한다.]

> Why(배경·근거·차별화)는 one-pager 참조: [링크]

**포지셔닝 한 문장:** [사내·외부의 "기존 X와 뭐가 다른가"에 답하는 한 문장]

**이 빌드의 성격:** [ ] 정식 기능 [ ] PoC/실험 — PoC라면: 목적은 기능 완성이 아니라 [무엇]의 검증이며, 성패는 6절 지표로 판정한다.

---

## 1. 대상 사용자 및 권한

<!-- 역할이 단순하면 표 대신 2~3문장. 권한·계정 요건이 갈리는 순간 표로. -->

| 역할 | 정의 | 계정 요건 | 핵심 권한 |
|---|---|---|---|
| | | | |

**범위 밖 사용자:** [이번 버전이 명시적으로 다루지 않는 세그먼트]

---

## 2. 핵심 개념 모델 및 용어

<!-- 객체가 3개 이상이거나 계층·복제·상태가 있으면 반드시 작성.
     우리가 겪은 사고: F4는 "모둠당 캔버스 1개", F6은 "모둠별 썸네일들" — 같은 문서가 다른 모델을 전제.
     이 절이 그 사고를 막는다. -->

```
[최상위 객체]
 └── [하위 객체] (생성 시점·수량 제약)
      └── ...
```

- **[용어A]**: [정의. 상태가 있으면 상태 목록까지]
- **[용어B]**: [정의]

**용어 금지/통일:** [기존 제품과 충돌하는 용어가 있으면 여기서 못박는다. 예: "보드" 대신 "프로젝트"]

---

## 3. 기능 정의

### 한눈에 보기

| ID | 기능 | 한 줄 정의 | 우선순위 |
|---|---|---|---|
| F1 | | | P0 |
| F2 | | | P0 / P1 |

<!-- P0 = 없으면 핵심 문제가 안 풀림(이것만으로 출시 가능해야 함) / P1 = 빠른 후속 / P2 = 설계만 대비.
     "전부 P0"는 P0가 없다는 뜻 — 매 기능에 "이거 빼면 정말 출시 못 하나?"를 물을 것. -->

### 기능 상세

**F1. [기능명]** *(대상 역할)*

- **필수 정보**: 이 화면/기능이 반드시 노출해야 하는 정보 [목록]
- **필수 행동**: 사용자가 반드시 할 수 있어야 하는 것 [목록]
- **플로우/상태**: [상태별 분기. 예: 데이터 0건일 때 / 1건 이상일 때]
- **품질 기준**: [숫자로. 예: 반영 2초 이내]
- **하지 않는 것**: [이 기능 안에서의 명시적 제외 — 비목표와 별개로 기능 단위 경계]

---

## 4. 사용자 스토리 및 완료 조건

<!-- 스토리는 "[역할]이 [목적]을 위해 [무엇]을 한다". 완료 조건은 체크박스, 전부 검증 가능하게. -->

| 스토리 | 완료 조건 |
|---|---|
| [역할] [무엇을 한다] (F1) | □ [검증 가능한 기준 1] □ [기준 2] |

---

## 5. 핵심 가정 (검증형 프로젝트만)

<!-- PoC가 아니면 이 절 삭제. 가정 = "이게 거짓이면 제품이 무너지는 믿음".
     각 가정에 검증 지표를 묶는다 — 그 지표의 정의·목표치는 다음 절(6절)에서 이어진다. -->

| # | 가정 | 검증 지표 | 위험도 |
|---|---|---|---|
| A1 | | | 높음/중간/낮음 |

---

## 6. 성공 지표 및 기각 기준

<!-- 5절 가정을 숫자로 판정하는 절 — 지표마다 어느 가정(A#)을 검증하는지 표시.
     측정 시작 시점·기간·측정 방법(어떤 로그, 어떤 키)까지 써야 지표다.
     목표치를 못 정하는 상태라면 "OQ-NN 확정 후 고정"으로 종속을 명시 — 모수 미정인데 절대값 기각선을 두지 말 것. -->

**선행 지표 (일~주 단위):**
- [지표명] = [정의] · 검증 대상 [A#] · 목표 [N%, [잠정] 여부] · 측정 [기간/방법]

**후행 지표 (주~월 단위):**
- [재사용·리텐션 계열 — 무엇을 "재사용"으로 정의하는지 명시]

**기각(킬) 기준:** [기간] 후 [조건 AND/OR 조건] → [무엇을 재검토/중단하는지]

---

## 7. 비목표 (Non-goals)

| # | 하지 않는 것 | 이유 (한 줄) | 재검토 |
|---|---|---|---|
| 1 | | | v2 후보 / 영구 제외 |

---

## 8. 제약 및 NFR

<!-- NFR = 기능이 "무엇을 하나"가 아니라 "얼마나 잘·어떤 조건에서 작동해야 하나"(품질 조건).
     범주 하나당 한 줄, 숫자나 원칙으로. 값을 못 정한 항목은 지어내지 말고
     그 값을 정해 줄 OQ 번호를 달아 "TBD — OQ-NN과 함께 산정"으로 쓴다.

     예시:
     - 성능: 편집 반영 2초 이내, 입장 로딩 3초 이내(4G+)
     - 규모: 세션당 최대 10명 / 동시 세션 수 TBD — OQ-02와 함께 산정
     - 플랫폼: 태블릿·크롬북·스마트폰 브라우저, 특정 OS 종속 금지
     - 언어: 한국어 전용
     - 개인정보: 수집 항목·미성년 해당 여부·보존/삭제 정책 — 별도 줄 필수(미정이면 OQ로)
     - 비용: TBD — OQ-02와 함께 산정 -->

- **[범주]**: [수치 또는 원칙]

---

## 9. 오픈 퀘스천

**착수 전 결정 (blocking):**

| # | 질문 | 왜 blocking인가 |
|---|---|---|
| OQ-01 | | [이게 안 정해지면 무엇이 막히는지] |

**설계 단계 결정:**

| # | 질문 | 비고 |
|---|---|---|
| OQ-11 | | [유력 후보안이 있으면 명시] |

---

## 부록. 변경 이력

<!-- 본문에는 이력을 남기지 않는다. 여기 한 줄씩만. -->

| 버전 | 일자 | 핵심 변경 | 해소/추가된 OQ |
|---|---|---|---|
| v1 | YYYY-MM-DD | 최초 작성 | — |
